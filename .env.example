# Authorized playback/embed provider base URL.
# Because PlayerOverlay is client-side, expose it with the NEXT_PUBLIC_ prefix.
# The player appends /movie/{tmdb_id} or /tv/{tmdb_id}/{season}/{episode}.
NEXT_PUBLIC_BASE_EMBED_URL=https://your-authorized-provider.example/embed
